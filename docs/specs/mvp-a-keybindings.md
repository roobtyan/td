# TD MVP-A Keybindings

## Global

- `Tab`：切换焦点（左栏 / 右栏）
- `j` / `k`：上下移动
- `Enter`：在左栏确认切换视图

## Clipboard

- `p`：规则解析剪贴板并创建任务
- `Ctrl+A`：AI 解析剪贴板（失败自动回退规则解析）

## Notes

- MVP-A 不包含 triage / expand 模式键位。
- 日志与回收站只提供基础浏览与生命周期命令联动。
